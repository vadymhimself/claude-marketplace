# Python
__pycache__/
*.pyc
*.pyo

# macOS / editor noise
.DS_Store
.idea/
.vscode/

# Session temp / locks that sneak in from Cowork mounts
.~lock.*#
*.tmp

# Packaged .plugin zips (built by scripts/pack.sh, uploaded to GitHub Releases)
dist/

# === KB credentials and bootstrap artifacts ===
# Each user/admin generates these on their own machine; never commit.
**/.kb-config
**/.kb-bootstrap.env
**/.token
**/.write-token
**/.url
**/generated/
